class Paths {
    @:deprecated("Functionality for this hasn't been implemented. Use Res for now!")
    public static inline function image(key:String):String {
        // return Reflect.field(hxd.Res, key).toTile();
        return null;
    }
}