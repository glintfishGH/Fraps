import hxd.Res;
import h2d.Tile;
import h2d.Bitmap;

class Note extends Bitmap {
    public function new(time:Float) {
        super();
    }

    public static function loadNotes(isPixel:Bool) {
        
    }
}